package com.z2xinyu.collection.set;

import org.junit.Test;

/**
 * HashSet不保证迭代顺序,不允许重复元素
 *
 * 元素唯一性通过：HashCode()方法和equals()方法
 *
 * put(K key, V value)，获取存入元素的hash值，如果相同，再equals比较，还是相同就替换
 *
 * @author zhangxinyu
 * @date 2023/7/10
 **/
public class HashSetTest {

    @Test
    public void add() {

    }
}
